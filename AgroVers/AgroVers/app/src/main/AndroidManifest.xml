<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.AgroVers">

        <activity android:name=".ProfileActivity" />
        <activity android:name=".AnalysisActivity" />
        <activity android:name=".WeatherActivity" />
        <activity android:name=".SoilMonitoringActivity" />
        <activity android:name=".CropSchemesActivity" />
        <activity android:name=".BuyingSellingActivity" />
        <activity android:name=".PestDiseaseActivity" />
        <activity android:name=".CropRecommendationActivity" />
        <activity android:name=".DashboardActivity" />

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>

